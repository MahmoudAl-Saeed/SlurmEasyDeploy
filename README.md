# Slurm Lab Automation Workspace

This workspace contains the publishable project in:
- `slurm-lab-status-portal/`

Supporting local artifacts (`dist/`, `tools/`) are intentionally excluded from source control by `.gitignore`.
